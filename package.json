{
  "name": "cfa-reedfrost-rs",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build --target web",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc -b frontend && vite build",
    "check": "tsc -b frontend && prettier . --check",
    "format": "prettier frontend -w",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript": "~5.8.3",
    "vite": "^7.0.3"
  }
}
